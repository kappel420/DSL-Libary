plugins {
    id 'groovy'
}

repositories {
    jcenter()
}

dependencies {
    implementation 'org.codehaus.groovy:groovy-all:2.5.14'
}

sourceSets {
    main {
        groovy {
            srcDirs = ['src', 'vars']
        }
    }
}
